TravelSense publish check.

If you can read this at https://YOUR-SITE.netlify.app/health.txt then Netlify
is publishing your files correctly and index.html should load at /.

If this returns 404, Netlify is serving the wrong location. Check that:
  1. netlify.toml is at the ROOT of the repository.
  2. Base directory is EMPTY in Netlify's build settings.
  3. All six files are at the repo root with no folders.

Safe to delete this file once everything works.
